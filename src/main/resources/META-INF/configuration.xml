<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!--表明重用预编译的SQL语句 -->
	<settings>
		<setting name="defaultExecutorType" value="REUSE" />
		<!-- <setting name="logImpl" value="LOG4J"/> -->
	</settings>
	<!--类型别名定义。今后可只用Student来代替它冗长的 全限定名 -->
	<typeAliases>
		<typeAlias alias="Customer" type="jp.co.sysevo.model.Customer" />
	</typeAliases>
	<!--environments可包含多个environment元素。 每个environment配置与数据库交互的细节，这里 只需要配置一个。default属性是指在创建SqlSessionFactory 
		时，若没有明确指定要用哪个environment，则使用此 属性指定的 -->
	<environments default="development">
		<environment id="development">
			<transactionManager type="jdbc" />
			<dataSource type="UNPOOLED">
				<property name="driver" value="com.mysql.jdbc.Driver" />
				<property name="url" value="jdbc:mysql://localhost:3306/test" />
				<property name="username" value="root" />
				<property name="password" value="rontech" />
			</dataSource>
		</environment>
	</environments>
</configuration>